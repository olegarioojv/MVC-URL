<?php

namespace app\controllers\portal; // Define o namespace da classe ProdutoController

// Define a classe ProdutoController
class ProdutoController 
{
    // Método para exibir a lista de produtos (ainda não implementado)
    public function index()
    {

    }

    // Método para exibir detalhes de um produto específico (ainda não implementado)
    public function show()
    {
        
    }

    // Método para mostrar o formulário de criação de um novo produto (ainda não implementado)
    public function create(): void
    {
        
    }

    // Método para armazenar um novo produto (ainda não implementado)
    public function store()
    {
        
    }

    // Método para editar um produto existente (ainda não implementado)
    public function edit($id)
    {
        
    }
}
